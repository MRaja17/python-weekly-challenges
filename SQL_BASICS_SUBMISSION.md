# SQL Basics — Assignment Submission

## Easy 1 — Key concept (in my own words)
SQL (Structured Query Language) is used to manage and query data stored in relational databases.
It allows selecting, filtering, grouping, and joining data stored in tables using simple commands.

---

## Easy 2 — Toy example
Retrieve all users older than 25.

```sql
SELECT name, age
FROM users
WHERE age > 25;
